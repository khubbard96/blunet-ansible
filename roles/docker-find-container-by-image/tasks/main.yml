---
- name: Get infos on container
  community.docker.docker_host_info:
    containers: true
  register: result

- name: Print information about containers
  ansible.builtin.debug:
    var: result.containers
  when: result is defined

