---
- name: Get infos on container
  community.docker.docker_host_info:
    containers: true
  register: result

- name: Print container names
  ansible.builtin.debug:
    msg: "{{ result.containers | map(attribute='Names') | flatten | select('search','^/blunet-minecraft-volume-interface') | flatten }}"
  when: result is defined

